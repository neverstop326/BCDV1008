const colors = ['red', 'green', 'blue'];

const capitalizedColors = words => words.map(word => word[0].toUpperCase() + word.substring(1))

console.log(capitalizedColors(colors));


// const capitalizedColors = colors.map(x => )